<!-- Footer Section Begin -->
<footer class="footer">
    <div class="page-up">
        <a href="#" id="scrollToTopButton"><span class="arrow_carrot-up"></span></a>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-lg-6">
                <div class="footer__nav">
                    <ul>
                        <li class="active"><a href="{{ path('app_homepage') }}" style="color: white;">Accueil</a></li>
                        {% if app.user %}
                            <li><a href="{{ path('app_profile') }}" style="color: white;">{{ app.user.username }}</a></li>
                        {% else %}
                            <li><a href="{{ path('app_login') }}" style="color: white;">Connexion</a></li>
                            <li><a href="{{ path('app_register') }}" style="color: white;">Inscription</a></li>
                        {% endif %}
                        <!-- Ajout du lien vers la page de suggestion -->
                        <li><a href="{{ path('app_suggestion') }}" style="color: white;">Suggérer un film</a></li>
                    </ul>
                </div>
            </div>
            <div class="col-lg-6">
                <p style="color: white;">Créé en 2024 par Lucas</p>
            </div>
        </div>
    </div>
</footer>
<!-- Footer Section End -->
